/**
 * The main Invisible XML classes.
 *
 * <p>In brief:</p>
 * <ul>
 *     <li>Construct an <code>InvisibleXmlParser</code> using one of the static methods in <code>InvisibleXml</code>.</li>
 *     <li>Use the parser to parse an input file, returning an <code>InvisibleXmlDocument</code>.</li>
 *     <li>Get details about the parse, including representations of one or more parses from the document.</li>
 * </ul>
 */
package org.nineml.coffeefilter;